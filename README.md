#A simple hangman playable on terminal.

##Things that I could improve:
-Turn the fixed size array of alreadyTypedChars to a dynamic array;
-Implement a more indipendent clearScreen function. The actual appends clear command on shell history
-Add colors;
